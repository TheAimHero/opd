import { HospitalIcon } from 'lucide-react';

import type { SidebarItems } from '@/types/sidebar';

export const visitSidebarItems: SidebarItems = {
  icon: <HospitalIcon />,
  title: 'Visit',
  url: '/visit',
};
