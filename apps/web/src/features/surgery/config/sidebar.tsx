import { ActivityIcon } from 'lucide-react';

import type { SidebarItems } from '@/types/sidebar';

export const surgerySidebarItems: SidebarItems = {
  icon: <ActivityIcon />,
  title: 'Surgery',
  url: '/surgery',
};
